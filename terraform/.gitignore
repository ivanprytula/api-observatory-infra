# Terraform state — never commit
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/

# Override files (local workspace config)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive variable files
*.tfvars
!*.tfvars.example

# Backend config (contains endpoint and bucket choices)
backend.local.hcl
!backend.local.hcl.example
backend.hcl
!backend.hcl.example

# Plan output
*.tfplan
*.plan

# Crash log
crash.log
