---
- name: 'Start Docker service'
  ansible.builtin.service:
    name: 'docker'
    state: started
  become: true

- name: 'Restart Docker service'
  ansible.builtin.service:
    name: 'docker'
    state: restarted
  become: true
