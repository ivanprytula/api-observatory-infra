---
# Bootstrap the private, SSM-operated AWS Stage 0 host. It does not deploy an
# image; app-owned manual CD supplies immutable digests after CI succeeds.
- name: Bootstrap AWS Stage 0 Compose host
  hosts: aws_dev
  become: true

  roles:
    - common
    - docker
    - stage0
